[build]
  base = "."
  publish = "packages/web/dist"
  command = "pnpm install --frozen-lockfile && pnpm -F @vet/shared build && pnpm -F @vet/web build"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "11.0.4"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
