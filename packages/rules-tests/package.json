{
  "name": "@vet/rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "@vet/shared": "workspace:*",
    "firebase": "^10.13.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
